# 30路口比赛演示运行手册

## 演示链路选择

主展示采用现有且最稳定的链路：

```text
frontend/pymarl --TCP 5000--> frontend/CitySimulation
```

Unity负责30路口场景、车辆、信号和指标展示，PyMARL通过长度前缀JSON协议执行
`init/reset/step/close`。C后端的REST/WebSocket服务作为独立能力展示：

```text
客户端 --REST/WebSocket 8000--> server/api_server.py --TraCI--> SUMO xiongan_30
```

端口8765仅用于SUMO可视化协议，不与上述两条会话混用。正式 DQN 权重已经交付；
演示中应明确区分真实模型预测、固定配时基线和手工动作。

## 演示前自动验收

在仓库根目录运行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build_and_validate.ps1
& D:\ChallengeCup2026\.venv-xiongan-c\python.exe -m pytest -q
```

预期为30/30路口契约通过及全部测试通过。

## Unity主演示

1. Unity 2022.3.62f2打开 `frontend/CitySimulation`；
2. 打开 `Assets/Scenes/City.unity`；
3. 点击Play，等待日志出现120条道路、30/30信号灯和TCP 5000启动；
4. 在仓库根目录执行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\run_unity_smoke_test.ps1
```

5. 在Game视图展示完整30路口、信号灯、车辆及运行面板；
6. 需要观察时使用侧栏fold按钮收起或展开控制面板。

2026-08-13实机验收已通过：Unity返回30个联合智能体、510维状态，连续执行3步并
返回有效奖励。后续可重复运行同一脚本作演示前检查。

## C后端能力演示

启动：

```powershell
& D:\ChallengeCup2026\.venv-xiongan-c\python.exe server\api_server.py
```

展示REST创建会话、660维状态、J01-J30奖励与完整30动作提交，以及
`ws://127.0.0.1:8000/api/v1/ws`的状态广播。使用三个 `ready` model ID 时，
`/api/v1/model/predict` 应返回 `200`、30 个动作和 `shared-dqn-26x4-v1`；若返回
`MODEL_NOT_LOADED`，应检查是否误用了历史 `shared-dqn-generalization-100k-v1`
或模型文件是否缺失。

## 正式模型验收

- 正式 SB3 DQN 权重、SHA-256 和 `normalization=none` 已注册；
- 30 动作、确定性、动作掩码及真实 SUMO 动作提交闭环已通过；
- 详细复验步骤见 `docs/C_后端验收与REST测试指南.md`；
- 轻量化性能报告和 Unity 对 8000 端口的直接闭环仍待完成。
