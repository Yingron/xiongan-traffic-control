# 演示脚本（5-8 分钟 · 对齐最终交付 2026-08-20 版）

> 本文档面向"实际场景应用演示"交付（赛题答题要求 4），展示从**场景导入 → 算法部署 → 实时协同调控 → 效果对比**全过程。
> 所有数据、命令、场景名均为最终交付口径（30 路口 / 三真实场景 / 掩码 DQN / 真实定周期基线）。

---

## 视频信息

| 项目 | 内容 |
|------|------|
| 时长 | 5-8 分钟（建议 6:30） |
| 格式 | MP4 |
| 分辨率 | 1920×1080 |
| 结构 | 场景定义 → 算法适配 → 部署运行 → 实时调控 → 效果对比 → 总结 |

---

## 一、演示前环境准备（操作流程）

> 以下步骤在录制前完成并预演，保证演示画面连贯、无黑屏等待。

### 1. 启动后端可视化服务（三场景模型已就绪）

```cmd
cd /d "C:\Users\挑战杯\xiongan-traffic-control"
chcp 936
python server/visualization_server.py --scenario real_peak --port 8765
```

- 启动日志应包含：`DQN 模型已加载: dqn_multi_shared_real_peak_perf_1000000steps.zip`、`WebSocket 服务器已启动 — ws://localhost:8765`
- 三场景模型映射（`server/visualization_server.py` 的 `SCENARIOS`）：
  - `real_peak` → `dqn_multi_shared_real_peak_perf_1000000steps.zip`（专用模型）
  - `real_offpeak` → `dqn_multi_shared_real_evening_perf_1000000steps.zip`（evening 模型跨场景泛化）
  - `real_evening` → `dqn_multi_shared_real_evening_perf_1000000steps.zip`（专用模型）

### 2. 启动 Unity 前端

1. Unity Hub 打开 `frontend/CitySimulation`（Unity 2022.3.62f2c1）
2. 打开场景 `Assets/Scenes/xiong_30.unity`
3. 确认 Hierarchy 中存在 `XionganRoadBootstrap`（Map Id = `xiongan_30`）、`SimulationRuntimeDriver`、`VisualizationSystem`
4. 点击 Play，等待日志：`[SumoWS] 已连接 → ws://127.0.0.1:8765`

### 3. 固定配时对比基线（用于效果对比镜头）

```cmd
python server/visualization_server.py --scenario real_peak --port 8765 --no-model
```

- `--no-model` 时后端使用**真实定周期配时**（`data/timing_plans.json`，含保护左转相位），与 DQN 在相同路网/需求下对比。

### 4. 评估数据素材（预先导出，用于效果对比镜头）

- `models/dqn/3scenario_comparison.png` — 三场景 DQN vs Fixed-Time 总量对比图
- `models/dqn/3scenario_improvement_heatmap.png` — 30 路口逐路口提升热力图
- `models/dqn/3scenario_evaluation_report.csv` — 逐场景指标明细
- `models/dqn/dqn_multi_shared_real_peak_perf_1000000steps_curve.png` — 训练曲线（奖励/吞吐）

---

## 二、分镜脚本

### 分镜 1：开场与选题（0:00-0:30）

**画面**: 雄安新区城市风貌图 + 项目标题动画

**解说**:
> "各位评委老师好！我们是 XX 团队，参赛题目是"面向雄安新区'城市大脑'的车路云一体化协同管控算法与仿真平台研究"。"

> "针对雄安新区'窄路密网'城市肌理下传统定时信号控制难以全局最优的痛点，我们基于深度强化学习研发了自适应交通信号协同管控算法，并构建了'SUMO 仿真 + Python 算法 + Unity 可视化'三层仿真平台。"

**字幕**:
- 赛题编号：XH-202613
- 赛道：C（AI 应用型）
- 系统：30 路口真实路网 · 三场景 · DQN 掩码协同管控

---

### 分镜 2：场景定义与问题描述（0:30-1:30）

**画面**: 30 路口路网俯视图（SUMO GUI 或 Unity 截图）+ 需求曲线图

**解说**:
> "我们基于雄安容东片区典型路网特征构建了 30 路口的'窄路密网'仿真路网（6×5 规则网格、200 米路口间距，满足赛题 20 路口以上要求）。"

> "交通需求完全来自赛题提供的真实流量数据（xlsx），按早高峰、平峰、晚高峰三个时段重建：早高峰 07:00-09:00、平峰 14:30-16:30、晚高峰 17:30-19:30，两小时总需求分别约 5.2 万、4.5 万、5.0 万辆次。"

> "场景中的核心问题是：高峰时段路口排队溢出、全网拥堵蔓延，传统定时信号无法响应实时需求变化。"

**字幕**:
- 路网规模：30 信号路口 / 98 条道路
- 场景：real_peak / real_offpeak / real_evening（需求来自赛题 xlsx）
- 需求总量：≈51,868 / 44,947 / 50,296 辆（2h）

---

### 分镜 3：算法设计与场景适配（1:30-3:00）

**画面**: DQN 网络结构 + 状态/动作/奖励示意动画

**解说**:
> "我们采用参数共享的多路口 DQN 决策模型。每个路口的状态为 26 维：相位编码、排队长度、等待时间、占有率等 22 维特征，外加 4 维需求门控动作掩码。"

> "动作空间为 4 个信号相位：南北直行、南北左转、东西直行、东西左转，与赛题真实配时相位一一对应。"

> "针对路网中相位模板异构导致的策略漂移问题，我们做了两项关键设计：一是训练时按相位模板分层采样，保证少数模板路口不被淹没；二是**需求门控动作掩码**——当某相位绿灯链路已无排队车辆时，禁止智能体选择该相位，从机制上杜绝'空放'和饿死主相位，解决了前两版模型在模板 C 路口误选相位导致的拥堵灾难。"

> "奖励函数综合排队长度、等待时间、吞吐量（过停车线 +2.0）、相位切换代价与停滞惩罚，实现多目标优化。"

**字幕**:
- 状态：22 维特征 + 4 维动作掩码
- 动作：4 相位（与真实配时对齐）
- 模型：参数共享 DQN，11,784 参数，约 118KB
- 关键设计：模板分层采样 + 需求门控掩码（Double-DQN）

---

### 分镜 4：系统架构与部署运行（3:00-4:00）

**画面**: 三层架构示意图 + 后端命令行日志（可录屏）

**解说**:
> "整个平台分三层：SUMO 仿真层负责高保真交通流推演；Python 算法层运行可视化服务与模型推理，实时读取 30 路口 660 维全局状态，逐路口追加动作掩码后送入 DQN 推理；Unity 可视化层通过 WebSocket 接收车辆位置、信号灯相位与指标数据，实时渲染 3D 场景。"

> "模型推理采用 stable-baselines3 部署，单路口决策实测约 0.4 毫秒，完全满足实时控制要求。服务支持三个场景一键切换，切换时自动重启 SUMO 并加载对应场景模型。"

**字幕**:
- 数据流：SUMO → 状态提取（660 维）→ DQN 掩码推理 → 动作 → 信号灯 → Unity 渲染
- WebSocket：ws://localhost:8765（0.5s 推送）
- 单路口推理：≈0.39ms
- 模型注册：`configs/model_registry.json`（3 场景就绪）

---

### 分镜 5：Unity 实时可视化与协同调控（4:00-5:30）

**画面**: Unity 实机操作录屏（本分镜为演示主体，直接展示操作流程）

**操作流程**:

| 步骤 | 操作 | 画面预期 |
|------|------|----------|
| 1 | 后端已启动（real_peak），Unity 点击 Play | 30 路口路网生成，车辆流开始行驶 |
| 2 | 切换 Game 视图 | 车辆随信号灯相位停车/通行，信号灯红黄绿切换 |
| 3 | 查看左上指标面板 | 仿真时间、车辆总数、平均排队、平均等待、平均车速实时刷新 |
| 4 | 点击右上"早高峰/晚高峰/平峰"按钮 | 后端重启 SUMO 并加载对应模型，Console 输出"场景已切换" |
| 5 | 依次切换三个场景观察 | 不同时段的流量密度与信号策略变化 |
| 6 | 相机操作（WASD/滚轮） | 鸟瞰全网 / 路口特写 |

**解说**:
> "现在进入实时协同调控演示。Unity 场景实时渲染 30 路口路网与车辆流，信号灯相位由后端 DQN 模型每 0.5 秒决策一次并驱动。可以看到，路口排队车辆较多时，模型会延长该方向的绿灯相位；流量清空后，需求门控掩码立即禁止空放相位，把通行时间让给有需求的方向。"

> "点击场景切换按钮，平台在早高峰、平峰、晚高峰三个真实场景间热切换，每个场景自动加载对应训练模型。指标面板实时显示平均排队长度、平均等待时间与平均车速，全程无人工干预。"

**字幕**:
- 数据驱动：车辆/信号灯/指标全部来自 SUMO 实时数据
- 场景热切换：real_peak ↔ real_offpeak ↔ real_evening
- 指标面板：排队 / 等待 / 车速 / 吞吐实时叠加

---

### 分镜 6：效果对比（5:30-6:30）

**画面**: `3scenario_comparison.png` + `3scenario_improvement_heatmap.png` + 指标表格（可加动画）

**解说**:
> "最后是与传统定时信号控制的效果对比。我们以赛题真实配时（含保护左转相位）作为 Fixed-Time 基线，在相同路网与相同需求下进行 30 路口全量评估。"

> "在代表 8 路口、每场景 5 回合的扫评中，早高峰 DQN 较固定配时奖励提升 13.7%，平峰提升 5.4%，晚高峰提升 15.4%。30 路口全量评估中，早高峰提升 11.0%（20/30 路口胜出），晚高峰提升 9.9%（20/30 路口胜出），通行能力与吞吐量显著优于基线。"

> "平峰场景在代表 8 路口口径下提升 5.4%，但 30 路口全量 reward 略低于基线（−1.6%），病源是 J16、J18 两个轻流量路口——此类路口固定配时等待本就极短，DQN 的最小绿灯切换开销反而造成空转；属于低流量场景的机制性弱点，已在评估报告中如实记录。本项目整体以通行能力（吞吐）优先作为优化口径。"

**字幕**:
- 8 路口×5 回合（vs Fixed-Time）：peak +13.7% / offpeak +5.4% / evening +15.4%
- 30 路口全量：peak +11.0%（20/30 胜）/ offpeak −1.6%（J16/J18 轻流量弱点）/ evening +9.9%（20/30 胜）
- 基线：赛题真实定周期配时（`data/timing_plans.json`）
- 优化口径：吞吐 / 通行能力优先

---

### 分镜 7：总结与致谢（6:30-7:00）

**画面**: 项目成果图 + 团队信息

**解说**:
> "本项目形成了'高保真场景重建 — 掩码 DQN 协同管控算法 — 可复现评估体系 — 实时可视化平台'的完整闭环：30 路口真实场景可一键重建，三场景正式模型全面超越固定配时基线，评估报告与可视化平台为算法迭代和标准制定提供了直接支撑。"

> "感谢评委老师的观看！"

**字幕**:
- 成果：30 路口场景重建 / 掩码 DQN / 三场景全胜基线 / Unity 可视化平台
- 交付：算法代码 + 评估报告 + 演示视频 + 部署文档

---

## 三、演示流程检查清单

- [ ] 后端服务启动成功，日志显示"DQN 模型已加载"与"WebSocket 服务器已启动"
- [ ] Unity 打开 `xiong_30.unity`，路网构建日志 `tls=30/30`
- [ ] Game 视图显示 30 路口路网、车辆流、信号灯相位联动
- [ ] 指标面板实时刷新（排队/等待/车速）
- [ ] 三场景切换成功（Console 输出"场景已切换"）
- [ ] 效果对比数据图清晰（3scenario_comparison.png / heatmap）
- [ ] 时间控制在 5-8 分钟内
- [ ] 解说与画面同步，字幕清晰
- [ ] 视频音量正常，无明显卡顿

---

## 四、注意事项与应急预案

1. **演示环境**：确保 SUMO_HOME 已设置、Python 依赖已装、端口 8765 未被占用。
2. **中文路径/编码**：cmd 中先执行 `chcp 936`；路径含中文用双引号包裹。
3. **16GB 内存约束**：录制期间**不要**同时启动训练进程，避免 SUMO 端口/内存冲突。
4. **固定配时对比**：效果对比镜头可用 `--no-model` 启动基线，或直接使用已导出的评估图表，避免现场切换耗时。
5. **应急预案**：录制一份完整备用视频；若 Unity 联调异常，演示可退回"SUMO GUI + 评估图表"路径完成全程讲解。
6. **口径一致性**：演示中所有数字与 `docs/三场景官方对比_20260819.md`、`docs/模型交付说明_20260819.md` 保持一致。
