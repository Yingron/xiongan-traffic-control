# Mermaid 图表验收记录（2026-08-23）

渲染命令：

```powershell
npx --yes @mermaid-js/mermaid-cli@latest -i figures/<name>.mmd -o figures/<name>.png -b white
```

## 严格检查结果

| 图表 | 内容与箭头核对 | 渲染 | 可读性 | 评分 | 结论 |
|---|---|---|---|---:|---|
| `system-architecture` | Unity↔API、API→Edge、API↔TraCI↔SUMO 的方向正确 | PASS | 白底、横向布局清晰 | 9/10 | ACCEPT |
| `decision-sequence` | 场景选择、状态回传、30 动作回传、批量执行和快照刷新顺序正确 | PASS | 时序文本完整 | 9/10 | ACCEPT |
| `shared-dqn` | 22 维状态和 4 维掩码进入共享网络，Q 值经掩码 argmax 输出动作 | PASS | 模块与颜色分组清晰 | 9/10 | ACCEPT |
| `training-flow` | 30 路口状态拆分、批量执行、共享回放和网络更新的循环方向正确 | PASS | 自上而下阅读清晰 | 9/10 | ACCEPT |
| `container-deployment` | Compose 启动两服务，API→Edge 与 API→SUMO、只读卷关系正确 | PASS | 横向部署关系清晰 | 9/10 | ACCEPT |

检查项：

- `.mmd` 为可独立渲染的 Mermaid 源码；
- 对应 `.md` 以 Mermaid 围栏包装同一份源码；
- 每张 PNG 已由 Mermaid CLI 成功生成；
- 不存在反向箭头、缺失组件或与代码不符的服务关系；
- 采用白底、深色连接线和克制配色，适合报告打印与投影。
