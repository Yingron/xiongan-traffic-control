# Minimal runtime set for SUMO/TraCI, FastAPI and formal ONNX deployment.
# Training-only packages (PyTorch, SB3, TensorBoard and Matplotlib) stay in
# the repository-level requirements.txt and are intentionally not baked into
# the deployment image.
fastapi>=0.100.0
uvicorn>=0.23.0
numpy>=1.24.0,<2.0
traci
pydantic>=2.0.0
onnxruntime>=1.17.0
