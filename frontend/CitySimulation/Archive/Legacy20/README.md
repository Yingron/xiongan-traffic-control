# 旧 20 路口 Unity 资源归档

归档日期：2026-08-21

这里保留从 Unity 工程 `Assets` 中迁出的历史 20 路口地图与场景资源，避免在正式 30 路口演示时被误选。

- `Maps/`：`xiongan_20.json`、旧默认地图 JSON 与 `map.osm`；
- `Scenes/`：`xiong_20.unity`。

正式运行入口为 `Assets/Scripts/Maps/xiongan_30.json` 和 `Assets/Scenes/xiong_30.unity`。本归档目录位于 `Assets` 之外，不会被 Unity 自动导入或加载；若需追溯旧版，可手动取回其中完整文件及其 `.meta` 文件。
