# 项目进展总结 (8.7)

> 历史快照：本文保留8月7日当时的20路口成果和任务分配。当前正式契约已经升级为30路口（J01–J30、每路口22维、全局660维），本文中的20路口文件名和实测数字不得用于当前配置或验收。

### 总览

截至 8月7日，项目已完成**三场景路网构建、全栈算法与服务开发、大规模预训练、核心可视化系统搭建**四大核心模块。相比第二阶段，项目取得了突破性进展，尤其是在工程化落地、算法效果初现及 Unity 可视化集成方面。

---

### 已完成任务清单

#### 路网构建与优化 (B)

| 文件 | 功能 | 状态 |
|------|------|------|
| [xiongan_20.net.xml](file:///xiongan-traffic-control/sumo_files/xiongan_20.net.xml) | 20路口路网（4×4网格） | ✅ |
| [optimize_phase_plan.py](file:///xiongan-traffic-control/evaluation/optimize_phase_plan.py) | 路口配时方案优化器 | ✅ |
| [lane_mapping.json](file:///xiongan-traffic-control/docs/lane_mapping.json) | 20路口车道映射数据 | ✅ |
| [tls_mapping.json](file:///xiongan-traffic-control/docs/tls_mapping.json) | 20路口信号灯映射数据 | ✅ |

**路网参数与优化**：
- 完成了路网安全优化，消除了 SUMO 仿真中的 `Unsafe green` 冲突。
- 运行 [optimize_phase_plan.py](file:///xiongan-traffic-control/evaluation/optimize_phase_plan.py) 对比了三种配时方案，确定了均衡型（balanced）配时（30/12/30/12秒）为最优。
- 20路口 600秒集成仿真验证通过：1,527辆到达、0碰撞、平均等待 0.94秒。

#### 算法训练与验证 (A)

| 文件 | 功能 | 状态 |
|------|------|------|
| [run_3scenario_training.py](file:///xiongan-traffic-control/training/run_3scenario_training.py) | 三场景并行训练脚本 | ✅ |
| [3scenario_summary_*.json](file:///xiongan-traffic-control/models/dqn/) | 三场景训练汇总报告 | ✅ |
| [dqn_multi_shared_flat_perf_*.zip](file:///xiongan-traffic-control/models/dqn/) | 平峰场景 100万步模型权重 | ✅ |
| [multi_metrics_comparison.json](file:///xiongan-traffic-control/models/dqn/) | 算法指标对比数据 | ✅ |

**核心改进与进展**：
- **平峰场景**：成功完成 100万步训练（速度约 279 steps/s），模型已保存。

**初步训练效果**（1万步 DQN vs 基线，J01路口）：
- **平均等待时间**：降低 21.3%（对比 Fixed-Time）。
- **平均排队长度**：降低 4.1%。
- **平均通行时间**：降低 4.2%。
- **安全性**：仿真实现 0碰撞、0传送。

#### 后端服务与接口 (C)

| 文件 | 功能 | 状态 |
|------|------|------|
| [websocket_server.py](file:///xiongan-traffic-control/server/websocket_server.py) | 20路口 WebSocket 服务 | ✅ |
| [api_server.py](file:///xiongan-traffic-control/server/api_server.py) | FastAPI REST 服务 | ✅ |
| [C_WebSocket联调记录.md](file:///xiongan-traffic-control/docs/C_WebSocket联调记录.md) | WebSocket 联调报告 | ✅ |
| [C_REST模型接口预留记录.md](file:///xiongan-traffic-control/docs/C_REST模型接口预留记录.md) | 模型接口预留记录 | ✅ |

**核心服务完成度**：
- **WebSocket 服务**：已从单路口升级至 20路口，支持 440维状态/20奖励实时推送，与 REST API 实现联动。
- **模型推理接口**：`/api/v1/model/predict` 接口框架已搭建完成，预留了 Stable-Baselines3 DQN 的加载和推理逻辑，当前正等待 A 提交最终训练好的权重文件（`.zip`）及相关归一化信息。

#### Unity 可视化与集成 (A)

| 文件 | 功能 | 状态 |
|------|------|------|
| [RoadNetworkBuilder.cs](file:///xiongan-traffic-control/frontend/CitySimulation/Assets/Scripts/Builder/RoadNetworkBuilder.cs) | 20路口 Unity 路网构建器 | ✅ |
| [TrafficLightAnimator.cs](file:///xiongan-traffic-control/frontend/CitySimulation/Assets/Scripts/GameObjects/Entities/TrafficLightAnimator.cs) | 4相位信号灯动画 | ✅ |
| [VehicleService.cs](file:///xiongan-traffic-control/frontend/CitySimulation/Assets/Scripts/Runtime/Services/Vehicle/VehicleService.cs) | 车辆服务（行驶、跟随） | ✅ |
| [SimulationRuntimeDriver.cs](file:///xiongan-traffic-control/frontend/CitySimulation/Assets/Scripts/Runtime/Simulation_fortoy/SimulationRuntimeDriver.cs) | 仿真运行时驱动（车辆生成） | ✅ |
| [BridgeClient.cs](file:///xiongan-traffic-control/frontend/CitySimulation/Assets/Scripts/Runtime/Simulation_fortoy/BridgeClient.cs) | TCP 客户端（对接后端） | ✅ |
| [BackendClinet.cs](file:///xiongan-traffic-control/frontend/CitySimulation/Assets/Scripts/Runtime/Simulation_fortoy/BackendClinet.cs) | 后端客户端（支持Mock/Bridge模式） | ✅ |
| [TrafficVisualizationUI.cs](file:///xiongan-traffic-control/frontend/CitySimulation/Assets/Scripts/Runtime/Visualization/TrafficVisualizationUI.cs) | 可视化 UI 面板 | ✅ |

**核心可视化功能**：
- **路网与信号灯**：在 Unity 中成功构建 4×4 网格路网，并实现了 20 个路口的 4 相位信号灯自动切换与动画。
- **车辆可视化**：实现了车辆在路网中的自动生成、沿道路行驶、红灯停止及跟车逻辑，可动态维持至少 25 辆车在路网中行驶。
- **后端对接**：开发了 `BridgeClient` 和 `BackendClinet`，实现了从 Unity 到后端服务的 TCP 对接，并内置了 fallback 机制，支持在无后端时使用本地 Mock 数据。
- **实时状态面板**：开发了 UI 面板，可显示连接状态、场景切换（早/晚/平峰）和实时指标（如仿真时间、车辆数、平均排队等）。

---

### 项目进展状态

#### 已完成（核心功能）

| 模块 | 进度 | 说明 |
|------|------|------|
| 20路口路网构建与验证 | 100% | 含安全与配时优化 |
| 算法框架与验证 | 90% | 平峰场景100万步完成，早/晚高峰场景因资源中断 |
| 后端 WebSocket 服务 | 100% | 20路口全功能实现 |
| 后端 REST API 服务 | 100% | 核心接口完成，模型推理接口等待权重 |
| Unity 核心可视化 | 80% | 路网、信号灯已实现，车辆生成与贴地问题待修复 |
| Unity 后端对接 | 90% | TCP 客户端已就绪，等待真实后端联调 |

#### 进行中与待完成

| 模块 | 进度 | 依赖 | 阻塞风险 |
|------|------|------|----------|
| Unity 可视化调试 | 20% | 需要定位车辆生成/贴地问题 | 高 |
| 早/晚高峰大规模训练 | 10% | 需要优化训练脚本/硬件资源 | 高 |
| 全场景模型评估 | 20% | 需要早/晚高峰模型权重 | 中 |
| 模型接口集成测试 | 30% | 需要 A 交付模型权重 | 中 |
| Unity-后端全链路联调 | 10% | 需要真实后端与模型推理闭环 | 低 |

---

### 下一步行动

#### 短期（本周）

1. **解决早/晚高峰训练问题** (A任务 )
   - **问题分析**：排查 `3221226091` 崩溃原因（Windows 内存访问冲突），可能是多进程环境下 SUMO 端口冲突、内存泄漏或 GPU 显存不足。
   - **解决方案**：尝试增加系统虚拟内存、降低并行环境数（SubprocVecEnv 的 `n_envs`）、或优化训练代码中的资源释放逻辑。
   - **产出物**：稳定运行的早/晚高峰训练脚本。

2. **完成三场景大规模训练** (A任务)
   - 运行早高峰场景 50万+ 步训练 → 保存模型。
   - 运行晚高峰场景 50万+ 步训练 → 保存模型。
   - **产出物**：三个场景（早、晚、平峰）的最终模型权重文件（`.zip`）。

3. **Unity 可视化系统调试与修复** (B任务)
   - **问题修复**：排查并修复车辆无法正确在道路上生成（位置偏移、贴地失败）、信号灯视觉异常等问题。
   - **实施步骤**：
     - 检查 `SimulationRuntimeDriver.cs` 中 `SpawnVehicleOnRandomRoad` 方法的坐标计算逻辑，确保生成的车辆位置位于道路中心线上。
     - 检查 `VehicleService.cs` 中车辆移动逻辑，确保车辆沿道路行驶且不发生穿模。
     - 使用物理系统（Rigidbody + Collider）或更精确的位置计算来解决贴地问题。
   - **产出物**：可视化表现正常的 Unity 场景。

4. **完成模型接口集成** (C任务)
   - 接收 A 提交的模型权重、SHA-256 校验值及归一化配置。
   - 将模型注册到 `model_registry.json`。
   - 执行 `/api/v1/model/predict` 接口的真实模型加载与推理测试。
   - **产出物**：可用的模型推理 API。

#### 中期（下周）

1. **完成全场景模型评估** (A任务)
   - 对比早高峰模型 vs 早高峰流量、晚高峰模型 vs 晚高峰流量、平峰模型 vs 平峰流量的性能。
   - 生成最终的性能对比报告（DQN vs 固定配时 vs 自适应）。
   - **产出物**：多场景性能对比图表与报告。

2. **Unity 全链路联调** (B + C任务)
   - C 启动完整后端服务（REST + WebSocket）。
   - B 在 Unity 中开启 `BridgeTcp` 模式，对接真实后端。
   - 实现完整的控制闭环：Unity 仿真采集状态 → 发送后端 → DQN 决策 → 返回动作 → Unity 执行。
   - **产出物**：全链路运行的演示 Demo。

3. **边缘部署初步验证** (C任务)
   - 将平峰场景模型导出为 ONNX 格式。
   - 进行模型量化（Dynamic Quantization）测试。
   - 验证推理延迟是否满足 ≤100ms 的要求。
   - **产出物**：边缘部署验证报告。

#### 长期（后续）

1. **项目收尾与文档**
   - 撰写系统设计报告、实验评估报告。
   - 准备答辩 PPT 和演示视频。
   - 整理完整的项目代码和文档。

---

### 协作依赖关系

```
A (模型权重) → C (模型接口集成) → B (全链路联调)
A/B (三场景模型/数据) → B (可视化集成)
C (后端服务) → B (Unity 对接)
```

| 依赖 | 前置条件 | 后续任务 |
|------|----------|----------|
| A→C | A 提交早/晚/平峰场景的最终模型权重 | C 完成 `/model/predict` 接口真实集成 |
| A→B | A 提供状态/奖励的最终定义与数据 | B 完成 Unity 指标面板的数据对接 |
| C→B | C 完成 WebSocket 与 `/model/predict` 接口 | B 完成 Unity 与后端的全链路联调 |

---

### 总结

项目已成功构建了**从算法原型到可视化工程系统的完整框架**：
1.  **路网与仿真**：高质量 20 路口路网搭建完成，配时方案经优化确定。
2.  **算法与训练**：核心 DQN 算法验证有效，平峰场景完成大规模训练，早/晚高峰场景的训练问题成为当前最大瓶颈。
3.  **后端与服务**：REST API、WebSocket 及模型推理服务框架全部就绪，静待真实模型上线。
4.  **可视化与集成**：Unity 端实现了精美的 20 路口路网、4 相位信号灯动画和动态车辆可视化，并已具备对接后端 TCP 服务的能力。

**当前项目的核心焦点在于完成全场景模型的交付，以打通全链路联调的最后一环。**
