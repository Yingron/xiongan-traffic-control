# 项目进展总结 (8.14 · 第四阶段)

> **历史阶段快照**：本文正文保留 2026-08-14 当时的进展与计划，不再代表当前交付状态。
> 截至 2026-08-20，peak/evening 正式 26 维掩码模型及 offpeak 泛化别名已经注册为
> `ready`，REST 正式模型闭环已通过。当前状态以
> `docs/模型交付说明_20260819.md`、`docs/三场景官方对比_20260819.md` 和
> `docs/C_后端验收与REST测试指南.md` 为准；本文中的 `waiting_for_A`、训练中和待模型
> 落盘描述仅用于阶段追溯。

### 总览

截至 **8月14日**，项目已完成**赛题对照审查、30路口真实路网迁移、真实需求场景重建、真实定周期基线、模型评估体系与三轮模型迭代（均匀→分层采样→动作掩码）**。相比 8.7 第三阶段，本阶段核心转变是：**抛弃自建 20 路口理想场景，全面转向赛题 xlsx 真实数据的 30 路口"窄路密网"场景**，并建立了"训练→扫评→根因分析→改进重训"的完整算法迭代闭环。

***

## 一、算法进展

### 1. 赛题对照全项目审查

对照赛题（XH-202613）逐条审查，确认并修复三大基础性问题：

| 问题                                   | 状态                 |
| ------------------------------------ | ------------------ |
| Fixed-Time 基线是假的（`(step//5)%4` 轮询）   | 已修复，改用赛题 xlsx 真实配时 |
| 路网仅 20 路口，且自建场景需求比真实低 7\~50 倍（无学习信号） | 已迁 30 路口 + 真实需求重建  |
| 评估/训练/部署链路上的 20 路口硬编码遗留              | 已全部清理              |

### 2. 30 路口真实路网与场景

- **路网迁移**：`sumo_files/xiongan_30.net.xml`（200m 规则网格、30 个信号路口，满足赛题"≥20 路口"要求，风格化"窄路密网"），旧 20 路口文件及自建 flat/morning/evening/low/high 场景全部删除。
- **真实需求生成**：`scripts/generate_real_demand_scenarios.py` 基于赛题 xlsx 逐路口真实 pcu 生成 `real_peak / real_offpeak / real_evening` 三场景（修复 flow begin 排序、对角路口映射等 3 个坑）。
- **基线容量标定（关键工作）**：真实配时在 xlsx 原始需求下全网死锁。根因诊断：200m 网格容量 ≈ 真实路网 45\~55%，且真实配时给保护左转仅 9\~33% 绿信比。按"基线可通行能力"重标定需求因子：**peak 0.48（51,868 辆/2h）、offpeak 0.60（44,947）、evening 0.42（50,296）**，7200s 冒烟最低车速 ≥ 2.0 m/s 全通。
- **验证**：`scripts/validate_network.py` 30/30 路口、4 相位、N/S/E/W 映射全过；env 状态冒烟 19/22 维非零。

### 3. 真实定周期基线（Fixed-Time）

- `baselines/fixed_time.py::RealFixedTimeController`：读 `data/timing_plans.json` 真实配时（绿/黄/全红）注入 SUMO 信号程序（`setProgramLogic`）。
- 相位名解析覆盖赛题全部命名（"东、北放行""东北、西南放行"等）；**右转随本进口直行相位放行**（自由放行实测产生碰撞，改为随直行后 0 碰撞）。
- 30 路口 × 3 时段 `--check-coverage` 全通过；J01 对照实测 queue 4.21→5.48、wait 168→420s、throughput 117→115，证实真实基线与赛题 rl4 基线同量级。

### 4. 训练管线修复（4 个阻塞 bug）

| Bug                               | 影响                                        | 修复                             |
| --------------------------------- | ----------------------------------------- | ------------------------------ |
| episode 截断单位错误（把 max\_steps 步数当秒） | 720 步 episode 只跑 720s，仅覆盖 2h 需求 10%，学不到拥堵 | 改按步计数，720 步=3600s，覆盖需求 49%     |
| `--scenario` 白名单拒绝 real\_\* 场景    | 无法启动真实场景训练                                | choices 改为动态读 SCENARIO\_CONFIG |
| Monitor `info_keywords` 类型错误      | n\_envs=1 分支崩溃                            | list→tuple                     |
| SUMO 端口并发互杀                       | 双训练互相杀进程                                  | 端口加 os.getpid()、stderr 落盘      |

实测 30 路口端到端训练速度 **\~54 steps/s**（1M 步 ≈ 5\~7h）。

### 5. 模型评估体系

- 新增 `evaluation/evaluate_model_sweep.py`：单模型 × 8 路口 × 5 回合 × 4 策略（DQN greedy/stochastic vs Fixed-Time）扫评，产出 JSON/CSV，种子可复现。
- `evaluate_3scenarios.py` / `evaluate_all_strategies.py` 全部切换到真实基线 + 30 路口契约。

### 6. 三轮模型迭代（real\_peak 场景）

**第 1 轮 · 均匀采样 1M**（7.7h）：总量 reward 与 FT 打平（+0.1%），等待 +64.9% 更差。根因分析：rl4 相位程序分 5 种模板（A×20/B×4/C×3/D×2/E×1，程序化推导自 net.xml），共享参数 DQN 被模板 A 主导，在模板 C 路口把 action\_0/3（几乎全红相位）当"主相位"误选——J05 等待 +7335%。

**第 2 轮 · 模板分层采样 1M**：`MultiIntersectionSharedEnv` 按模板加权采样（少数模板合计 55%，模板 C 两倍加权）。评估结果（8 路口聚合）：**reward 19,314 vs FT 17,537 = +10.1%，等待 −0.2% 持平**。模板 C 灾难根治：J05 −119%→−6%、J10 −32%→+39%；代价是 A/B 模板小幅回落（A 回合占比 66.7%→45%），曲线未收敛。

**第 3 轮 · 2M anticollapse 续训**（判定失败）：总量 +4.5% 反低于 1M，J10 灾难复发（reward 2151→596、等待 210s→9155s、action\_0 占比 49%）。**结论：分层采样救不回长期梯度漂移，2M 续训有害，保留 1M 分层模型；根治需硬约束。** 1M 分层模型已备份至 `models/dqn/archive/`。

### 7. 模板 C 需求门控动作掩码

在 2M 失败教训基础上实现硬约束方案：

- **认知校正**：用 `getControlledLinks` 逐链路核对，推翻"全红相位"认知——模板 C 的 4 个相位都服务真实需求，静态掩码会滞留真实车流；真实失败模式是策略锁定子集饿死东西向主相位 → 采用**需求门控动态掩码**（相位绿灯链路 halting 车辆 < 1 时该动作无效）。
- **实现**：观测 22→**26 维**（状态+4 掩码，公开 660 维契约不变）；新增 `training/masked_policy.py`（MaskedDQN：无效动作 Q−1e8、探索仅在有效动作内）；评估脚本兼容旧 22 维模型；API 推理侧支持掩码。
- 全部冒烟验证通过（掩码随排队动态翻转、保存/加载往返、旧模型兼容）。
- **real\_peak 1M 掩码版重训已启动**。

### 8. 奖励函数改进

- 新增吞吐量奖励（每辆过停车线 +2.0，`crossed_vehicles`）+ 权重再平衡（切换代价 0.05→0.01、停滞惩罚 0.02→0.08）+ 相位持续时长特征（state\[20]，60s 归一化）。
- monitor.csv 新增 `cleared_count` 列，训练曲线增加吞吐量子图。已验证但未与新模型同批对比。

***

## 二、后端进展

### 1. 30 路口服务契约迁移（8.12-8.13）

- `api_server.py` / `websocket_server.py` / `model_service.py` / `visualization_server.py` / `state_extractor.py` / `traci_service.py` 全部完成 **20→30 路口、440→660 维、v1-20x22→v1-30x22** 契约迁移。
- `visualization_server._extract_state` 重写为复用 `env/global_state.get_global_state`（基于重建的 `docs/lane_mapping.json` 几何映射），实测 660 维状态正确。
- `docs/接口文档.md`、`docs/Unity前端部署启动使用说明.md`、README 契约数字同步更新。

### 2. 动作掩码推理对接（8.14）

- `model_service.py::predict` 新增 `action_masks` 参数；`api_server.py` 新增 `inference_masks`（与状态同刻取掩码）；`visualization_server.py` 按模型维度自动追加掩码。
- 兼容旧 22 维模型（按 observation\_space 裁剪）。

### 3. 未完成项（转下阶段）

- `configs/model_registry.json` 仍为 `waiting_for_A`（等待掩码版新模型落盘后注册，observation\_dimension 更新为 26）。
- `/model/predict` 真实闭环、边缘轻量化（`edge_deploy/inference.py` 写死 26 维、`dqn_quantized.pt` 不存在、`train_distill.py` teacher 不存在）、Dockerfile（无 SUMO）均未动。

***

## 三、前端进展

**本阶段无代码提交**（最后提交 8.7 `038f377 增加Unity可视化系统`），状态停留在 8.7 总结：

- 已有：Unity 20 路口路网构建、4 相位信号灯动画、车辆生成/行驶/跟车、BridgeClient/BackendClient TCP 对接（含 Mock fallback）、实时指标 UI 面板。
- ⏸ 未推进：车辆生成位置偏移/贴地问题修复（8.7 时为 20%）、后端全链路联调。
- 相关脚本已适配：`scripts/convert_to_unity_map.py` 在 30 路口迁移中已更新（SUMO 路网 → Unity 地图导出），待前端组应用重建 30 路口 Unity 场景。

***

## 四、核心评估数据（real\_peak，8 路口 × 5 回合）

| 模型                 | reward 总量  | vs Fixed-Time | 等待时间          | 碰撞      |
| ------------------ | ---------- | ------------- | ------------- | ------- |
| Fixed-Time（真实配时）   | 17,537     | —             | 2,654s（基准）    | 0       |
| 均匀采样 1M            | 17,554     | +0.1%         | +64.9%        | \~2 次   |
| **分层采样 1M（当前正式）**  | **19,314** | **+10.1%**    | 2,649s（−0.2%） | \~1.4 次 |
| 2M 续训 anticollapse | 18,326     | +4.5%         | +342%         | 2.8 次   |

分层采样 1M 逐路口（vs FT）：胜 5/8 —— J01 +14%、J10 +39%、J20 +2.9%、J21 +38%、J25 +18%；败 3/8 但均为轻微回落 —— J05 −6%、J15 −5.5%、J30 −13%。

***

## 五、当前进行中

| 任务                   | 状态    | 预计产出                                                        |
| -------------------- | ----- | ----------------------------------------------------------- |
| real\_peak 掩码版 1M 重训 | 训练中   | `dqn_multi_shared_real_peak_perf_1000000steps.zip`（26 维掩码版） |
| 掩码版扫评                | 待训练完成 | 重点验证 J05/J10 不再误选、总量 vs 分层 1M 对比                            |

***

## 六、后续工作安排（按组分工）

> 提交 deadline：**9 月 15 日**（邮件提交：报告+PPT+代码+评估报告+演示视频）。剩余约 4.5 周。

### 算法组（gyr）

| 优先级 | 任务        | 内容                                                                                      | 产出物              | 时间        |
| --- | --------- | --------------------------------------------------------------------------------------- | ---------------- | --------- |
| P0  | 掩码版模型评估   | 训练完成后跑 `evaluate_model_sweep.py`，与分层 1M/FT 三方对比，定夺正式模型                                  | 扫评 JSON/CSV + 结论 | 8.14-8.15 |
| P0  | 三场景正式模型补齐 | 训练 real\_offpeak、real\_evening 各 1M（掩码版配置，\~5-7h/个，串行跑避免 16GB 内存互杀）                     | 三场景正式模型 ×3       | 8.15-8.18 |
| P0  | 正式三场景对比报告 | `evaluate_3scenarios.py --episodes 5 --max-steps 720`（DQN vs 真实 Fixed-Time，三场景 × 30 路口） | 正式对比数据与图表        | 8.18-8.20 |
| P1  | 扰动注入      | 基于已有 VSS（施工占道）补"大型活动"扰动场景并评估算法鲁棒性                                                       | 扰动场景 + 评估数据      | 8.20-8.24 |
| P1  | 实验素材补全    | 多参数/消融实验（奖励权重、掩码阈值）、时空轨迹图、指标曲线                                                          | 实验评估报告素材         | 8.20-8.30 |
| P0  | 文档撰写      | 系统设计与算法报告（≥20 页）、实验评估报告（≥30 页，含 10+ 图表）、答辩 PPT                                          | 三份文档             | 8.25-9.08 |

### 后端组 (lpq)

| 优先级 | 任务        | 内容                                                                                        | 产出物                      | 时间        |
| --- | --------- | ----------------------------------------------------------------------------------------- | ------------------------ | --------- |
| P0  | 模型注册与闭环   | 新模型落盘后更新 `model_registry.json`（observation\_dimension=26、SHA-256），`/model/predict` 真实推理测试 | 可用的模型推理 API              | 8.15-8.17 |
| P0  | 轻量化修复与实施  | 修 `edge_deploy/inference.py`、`train_distill.py` 的 26 维遗留；ONNX 导出 → 动态量化 → 结构化剪枝           | onnx/quantized/pruned 模型 | 8.17-8.24 |
| P0  | 轻量化性能验证   | 推理延迟（单路口 ≤100ms、30 路口并发 ≤500ms）、模型大小（≤1MB）、精度退化（≤5%）对比报告                                  | 轻量化验证报告                  | 8.24-8.28 |
| P1  | Docker 部署 | Dockerfile 补 SUMO 依赖、docker-compose、部署文档                                                  | 可复现部署包                   | 8.24-8.30 |
| P1  | 联调支撑      | 配合前端全链路联调（WebSocket 660 维 + REST 掩码推理）                                                    | 联调记录                     | 8.25-9.02 |

### 前端组 (zcx)

| 优先级 | 任务            | 内容                                                     | 产出物            | 时间        |
| --- | ------------- | ------------------------------------------------------ | -------------- | --------- |
| P0  | Unity 30 路口适配 | 应用 `convert_to_unity_map.py` 新导出，重建 30 路口 Unity 场景与信号灯 | 30 路口 Unity 场景 | 8.15-8.20 |
| P0  | 可视化调试收尾       | 修复车辆生成位置偏移/贴地、信号灯视觉异常                                  | 表现正常的可视化场景     | 8.15-8.20 |
| P0  | 指标面板真实数据对接    | UI 面板对接后端 660 维状态（含场景切换、掩码动作显示）                        | 真实数据 Dashboard | 8.20-8.25 |
| P0  | 全链路联调         | Unity 采集状态 → 后端 → DQN 掩码推理 → 动作回传 → 信号执行闭环             | 全链路 Demo       | 8.25-9.02 |
| P0  | 演示视频          | 录制 5-8 分钟视频：场景导入→算法部署→实时协同→效果对比，关键指标实时叠加，配解说           | 演示视频 + 脚本      | 9.01-9.10 |

### 跨组协作与收尾

| 任务                                 | 参与组   | 时间        |
| ---------------------------------- | ----- | --------- |
| 全链路联调与稳定性验证（无崩溃完整复现管控流程）           | 前端+后端 | 8.25-9.02 |
| 演示视频脚本与素材汇总                        | 全体    | 9.01-9.08 |
| 内部评审：答辩预演、材料互查（替换旧 20 路口/26 维错误描述） | 全体    | 9.05-9.10 |
| 最终打包（代码+报告+PPT+视频）与邮件提交            | 全体    | 9.10-9.14 |

### 协作依赖关系

```
算法组(模型权重) ──► 后端组(/model/predict 注册闭环) ──► 前端组(全链路联调)
算法组(评估数据) ──► 全体(实验报告/PPT/演示视频)
后端组(660维状态+掩码接口) ──► 前端组(指标面板/联调)
```

***
