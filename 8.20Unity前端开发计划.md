# Unity 前端开发计划（8.20 · 第五阶段启动）

> 前提：算法侧已于 8.19 最终交付（三场景正式模型 + 评估结论 + 前后端契约就绪）。
> 本阶段任务 = **开发 Unity 前端**，支撑赛题"数据采集与可视化模块 + 实际场景演示视频"两大交付。
> 提交 deadline：**9 月 15 日**，剩余约 3.5 周。

---

## 一、当前进度结论（8.20 代码勘察）

### 后端（已就绪，无需改动）

| 项 | 状态 |
| --- | --- |
| 30 路口路网 + 三真实场景 | `sumo_files/xiongan_30.net.xml` + real_peak/offpeak/evening.sumocfg（均挂 30 路网）✅ |
| 三场景正式模型 + 注册 | `models/dqn/` 2 个正式 zip，registry 3 ready + 1 archived ✅ |
| WebSocket 可视化服务 | `server/visualization_server.py` 端口 8765，0.5s 广播 `state` 消息 ✅ |
| 消息契约 | `{type, step, scenario, scenario_label, simulation_time, traffic_lights[], vehicles[], metrics, actions}`；traffic_lights 为 J01–J30 的 `phase(0-3)/phase_name(NS/EW Straight/Left)` ✅ |
| 场景名 | 后端只认 `real_peak / real_offpeak / real_evening`（发别的名字返回 error）✅ |

### Unity 前端（停滞在 20 路口，8.7 后无代码提交）

**已有**：20 路口路网构建、4 相位信号灯动画、WebSocket 客户端/可视化桥接（SUMO 数据驱动车辆渲染）、实时指标 UI 面板、TCP Bridge（旧路径）。

**与 30 路口契约的 6 处失配**（本次开发需逐项修复）：

| # | 位置 | 问题 | 影响 |
| --- | --- | --- | --- |
| 1 | `Assets/Scripts/Maps/` | 只有 `xiongan_20.json`，无 30 路口地图 | 场景仍是 20 路口 |
| 2 | `scripts/convert_to_unity_map.py` | 输出路径写死 `ChallengeCup-main/CitySimulation/...`（仓库中不存在） | 30 路口地图无法生成到正确位置 |
| 3 | `Builder/RoadNetworkBuilder.cs` | `intersectionsExpected = 20` 硬编码（L232/L275，L285 校验） | 加载 30 路口地图会报警"路口数 ≠ 20" |
| 4 | `Editor/RoadNetworkBuilderEditor.cs` + `Bootstrap/XionganRoadBootstrap.cs` | 菜单/默认 `mapId` 全为 `xiongan_20` | 无法一键构建 30 路口 |
| 5 | `Runtime/Visualization/SumoVisualizationBridge.cs` | ①信号灯查找硬编码 20 个路口 ID（J01–J20）与 20 路口坐标表；②日志写死 `"/20"`；③相位映射 `(phase<2)?0:2` 丢失左转相位区分 | 30 路口只能匹配到 J01–J20，J21–J30 信号灯不更新；左转绿灯视觉上等同直行 |
| 6 | `Runtime/Visualization/TrafficVisualizationUI.cs` | 场景切换按钮发送 `morning/evening/flat`（后端已改为 `real_*`） | **Unity 侧场景切换对真后端是坏的** |

**遗留待验证**：车辆贴地/生成偏移（8.7 时约 20% 车辆异常）；信号灯视觉（左转箭头缺失）。

**文档失配**：`docs/Unity前端部署启动使用说明.md` 场景段已更新为 real_*，但章节 7（场景配置）仍为 xiongan_20/20 路口；`docs/演示脚本.md` 仍为旧口径（7/20 路口、morning/evening/flat、知识蒸馏+INT8），与最终交付的 DQN 掩码模型 + 三场景真实对比数据不符，需重写。

---

## 二、后续开发内容（按优先级排期）

### P0-1 Unity 30 路口地图与场景重建（8.20–8.21，Python 侧可由 AI 直接完成）

| 任务 | 内容 | 产出物 |
| --- | --- | --- |
| 修转换脚本 | `scripts/convert_to_unity_map.py` 输出路径改为 `frontend/CitySimulation/Assets/Scripts/Maps/xiongan_30.json`（从真实 `xiongan_30.nod.xml` 读 J01–J30 坐标，生成 30 信号灯 + 6×5 网格道路） | `xiongan_30.json`（trafficLights=30，ID 与后端一致） |
| 桥接生成验证 | 跑转换脚本并抽查 JSON（30 信号灯、坐标与 nod.xml 对齐） | 转换结果校验 |
| Builder 泛化 | `RoadNetworkBuilder` 的 `intersectionsExpected` 改为从 JSON 实际 trafficLights 数动态计算（兼容旧 20 地图） | 代码修改 |
| Editor/Bootstrap 泛化 | 菜单加"构建 xiongan_30 30路口"（或 MapId 泛化），`XionganRoadBootstrap.mapId` 默认改 `xiongan_30` | 代码修改 |
| 30 路口场景 | 新建 `Scenes/xiong_30.unity`（复制 xiong_20 并改 Bootstrap 配置；含 XionganRoadBootstrap + SimulationRuntimeDriver + VisualizationSystem） | 新场景文件 |

### P0-2 可视化桥接 30 路口适配 + 视觉收尾（8.21–8.22）

| 任务 | 内容 |
| --- | --- |
| 信号灯查找泛化 | `SumoVisualizationBridge`：路口 ID 改为从地图 JSON/场景中动态获取（30 个），删除硬编码 20 路口坐标回退表；日志 `/{30}` |
| 相位映射保真 | SUMO 4 相位（NS直/NS左/EW直/EW左）→ Unity 4 相位；建议：`phase 0→0、1→0(标记左转)、2→2、3→2` 至少先打通，左转箭头灯列为 P1 增强 |
| 车辆贴地修复 | SUMO→Unity 坐标（scale=1，x∈[0,800]，z∈[0,1000]）验证；统一 `vehicleYOffset`（如 0.3）避免路面 z-fighting/半埋 |
| 冒烟 | Unity Play 下 30 路口 + 30 信号灯 + 车辆流 + 指标面板全部刷新 |

### P0-3 场景切换与指标面板真实对接（8.22–8.23）

| 任务 | 内容 |
| --- | --- |
| 场景切换修通 | `TrafficVisualizationUI` 按钮/标签改 `real_peak/real_offpeak/real_evening`；发 `switch_scenario` → 收 `scenario_switched` → 面板更新 |
| 指标面板补全 | 已展示：仿真时间/车辆数/平均排队/平均等待/平均车速/已到达/已发车；补：吞吐量、当前模型状态（后端 `connected` 消息带 `model_loaded`）；如需可显示动作/掩码 |
| 连接状态展示 | `connected` 消息含 `intersection_order`（30 ID），可用于前端校验信号灯数 |

### P0-4 全链路联调（8.24–8.28，前端+后端协作）

| 任务 | 内容 |
| --- | --- |
| 闭环验证 | Unity 启动 → WS 连接 → SUMO 状态 → DQN 掩码推理 → 动作回传 → 信号灯执行 → Unity 信号灯/车辆随相位变化 |
| 三场景切换 | Unity 内逐一切换 real_peak/offpeak/evening，验证后端重启 SUMO、模型切换、指标清空重启 |
| 稳定性 | 30 分钟连续运行无崩溃/断流；断线重连生效 |
| 记录 | 联调记录文档 + 问题清单 |

### P0-5 演示素材与文档（8.28–9.02）

| 任务 | 内容 |
| --- | --- |
| 更新部署说明 | `docs/Unity前端部署启动使用说明.md`：全部 xiongan_20/20 路口改 30；场景配置清单更新 |
| 重写演示脚本 | `docs/演示脚本.md`：对齐最终交付——26 维状态+4 掩码、4 相位、三场景真实对比（8 路口 +13.7%/+5.4%/+15.4%，30 路口 +11.0%/+9.9%）、掩码机制；分镜 7（Unity 可视化）扩写操作步骤 |
| 录屏素材 | Unity 内录制：三场景动画 + 指标面板实时叠加 + 场景切换 + 效果对比画面 |

### P1 增强（9.02–9.10，时间允许再做）

| 任务 | 内容 |
| --- | --- |
| 左转箭头灯 | `TrafficLightAnimator` 扩展为 5 灯头（红/黄/绿 + 左转黄/绿），按相位点亮，提升视觉保真 |
| 相机与特效 | 预设相机视角（全景/路口特写）、演示用无人机视角路径 |
| 性能 | 车辆池上限、LOD、实例化渲染（30 路口 200m 网格 ~2000 辆车场景帧率） |

### 跨组协作

| 项 | 依赖 | 时间 |
| --- | --- | --- |
| 全链路联调（无崩溃完整复现管控流程） | 前端 P0-1~P0-3 完成后 | 8.24–9.02 |
| 演示视频录制（含解说与指标叠加） | 联调稳定后 | 9.01–9.10 |
| 内部评审（替换旧 20 路口/旧场景名表述） | 全体 | 9.05–9.10 |
| 最终打包提交 | 全体 | 9.10–9.14 |

---

## 三、与赛题/plan 的对应关系

| 赛题条目 | 对应开发项 | 评分点 |
| --- | --- | --- |
| 功能二-② 数据采集与可视化模块 | P0-1~P0-3（30 路口可视化 + 指标面板） | 系统实现与功能完成度（30/40 分） |
| 功能二-③ 平台部署与性能优化 | P1（性能/帧率优化，可选） | 工程化潜力 |
| 答题要求 3 可视化图表 | P0-3 指标面板 + 后端评估图表 | 实验验证与性能评估（20 分） |
| 答题要求 4 演示视频 5–8 分钟 | P0-4 联调 + P0-5 录屏 + 脚本 | 实际场景演示效果（10 分） |
| 需求（2）云-边-端协同 | 全链路闭环（Unity 边端可视化 ↔ 后端云端调度） | 创新性/架构 |

---

## 四、可直接由 AI 代做的部分（无需 Unity 编辑器）

1. `scripts/convert_to_unity_map.py` 输出路径修复 + 生成 `xiongan_30.json` 并校验
2. `docs/Unity前端部署启动使用说明.md`、`docs/演示脚本.md` 更新
3. `RoadNetworkBuilder.cs` / `SumoVisualizationBridge.cs` / `TrafficVisualizationUI.cs` 等 C# 改动的代码编写（需在 Unity 编辑器内编译验证）
4. 联调辅助：后端服务启动命令、`scripts/smoke_test_api.py` 冒烟复用

> C# 改动无法在本环境编译验证，落地时需在 Unity 2022.3.62f2c1 打开工程确认 Console 无编译错误。
